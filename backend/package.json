{
  "name": "@highland-ai/claude-setup-backend",
  "version": "1.0.0",
  "description": "Highland AI - Claude Setup Backend (AWS Lambda)",
  "type": "module",
  "scripts": {
    "dev": "sam local start-api",
    "build": "esbuild functions/**/*.js --bundle --platform=node --target=node20 --outdir=dist --format=esm",
    "deploy": "sam deploy --guided",
    "test": "node --test",
    "lint": "eslint functions/"
  },
  "keywords": [
    "lambda",
    "aws",
    "serverless"
  ],
  "author": "Highland AI",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.9.1",
    "@aws-sdk/client-s3": "^3.461.0",
    "@aws-sdk/client-dynamodb": "^3.461.0",
    "@supabase/supabase-js": "^2.38.4",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.8",
    "eslint": "^8.55.0",
    "@aws-sdk/client-lambda": "^3.461.0"
  }
}
